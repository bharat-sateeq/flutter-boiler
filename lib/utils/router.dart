import 'package:sateeq/views/pages/Home.dart';

Object appRoutes = {
  '/': (context) => const Home(),
  //'/auth': (context) => Router(),

  // pages

  // auth
  // '/login': (context) => LoginPage(),
  // '/dashboard': (context) => DashboardPage(),

  // backend
};
